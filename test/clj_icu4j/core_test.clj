(ns clj-icu4j.core-test
  (:require [clojure.test :refer :all]
            [clj-icu4j.core :refer :all]))


(def s1 " This is a sentence. This is another sentence. இது ஒரு வாக்கியம்.  இது அடுத்த வாக்கியம்.  எப்படி இருக்கிறது? ")

(def s2 "ஐயோ")

(def s3 " ஐயோ")

(def s4 "ஐயோ ")

(def s5 " ஐயோ ")

